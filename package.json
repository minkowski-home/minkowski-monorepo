{
  "name": "minkowski-monorepo",
  "private": true,
  "version": "0.0.0",
  "description": "Workspace for Minkowski apps, services, and shared packages.",
  "workspaces": [
    "apps/corporate-website/frontend",
    "apps/design-your-space/frontend"
  ],
  "scripts": {
    "lint": "turbo run lint",
    "test": "turbo run test",
    "build": "turbo run build"
  },
  "devDependencies": {
    "turbo": "^1.13.4"
  }
}
