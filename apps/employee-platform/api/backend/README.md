# Employee Platform Backend (Legacy)

This directory previously hosted the FastAPI backend for the employee platform. The active API now lives in `apps/employee-platform/api/server` (Node.js + TypeScript), and AI endpoints are served from `services/ai-agents`.

## Next steps
- Keep this folder for historical reference only.
