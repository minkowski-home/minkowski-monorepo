service: design-your-space-api
indicators:
  availability:
    target: 99.5
    indicator: "5xx_rate < 0.5%"
  latency:
    target_ms: 400
    percentile: P95
notes: |
  Former README mentioned dashboards + alerts; this file links the two by
  capturing the high-level SLO definitions.
